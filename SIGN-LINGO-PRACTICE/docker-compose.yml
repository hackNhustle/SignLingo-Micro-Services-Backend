version: '3.8'
services:
  practice-service:
    build: .
    container_name: practice-service
    env_file: .env
    command: uvicorn main:app --host 0.0.0.0 --port 5004
    networks:
      - signlingo-mesh

networks:
  signlingo-mesh:
    external: true
