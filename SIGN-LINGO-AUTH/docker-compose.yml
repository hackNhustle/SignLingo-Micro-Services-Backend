version: '3.8'
services:
  auth-service:
    build: .
    container_name: auth-service
    env_file: .env
    command: uvicorn main:app --host 0.0.0.0 --port 5002
    networks:
      - signlingo-mesh

networks:
  signlingo-mesh:
    external: true
